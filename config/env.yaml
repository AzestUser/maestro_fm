# Default environment variables for local runs and CI.
# Pass to Maestro CLI: maestro test -e KEY=value flows/
APP_ID: com.maudau

# Web
WEB_BASE_URL: https://maudau.com.ua
# Override for staging: maestro test -e WEB_BASE_URL=https://staging.maudau.com.ua flows/web
